# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Database (async PostgreSQL for Neon)
sqlalchemy>=2.0
asyncpg>=0.29

# Settings / validation
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.0

# Auth (login/signup)
bcrypt>=4.1
python-jose[cryptography]>=3.3

# RAG — vector search (Qdrant) + embeddings (Gemini) + LLM calls (OpenRouter)
qdrant-client>=1.9
google-generativeai>=0.8
httpx>=0.27

# Rate limiting + env loading
slowapi>=0.1.9
python-dotenv>=1.0
